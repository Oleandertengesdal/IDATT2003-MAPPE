package idi.edu.idatt.mappe.models.enums;

/**
 * Represents the type of tile on the board
 * <p>
 *     This is going to be used in The Lost Diamond, where we have different types of tiles.
 *     We have, CITY, SEA_ROUTE, LAND_ROUTE, STARTING_CITY, FLIGHT_ROUTE.
 */
public enum TileType {
    CITY,
    SEA_ROUTE,
    LAND_ROUTE,
    STARTING_CITY,
    FLIGHT_ROUTE
}
